//输入
int N,M;
char field[MAX_N][MAX_M+1];//园子
//这里为什么加一？是因为如果读取一个字符串，那么就会多一个\0，这个加一，就是为多出来的\0留的？
//这里我可以试一下直接读取一个字符串
void input() {
	int i,j;
	scanf("%d %d",&N,&M);
	for(i=0;i<N;i++) {
			scanf("%s",&field[i][0]);
	}
}
